package com.example.aiadventchalengetestllmapi.aistateagent

internal const val VALIDATION_FORMAL_PROMPT_TEMPLATE = """Ты — ИИ-агент, выполняющий формальную проверку результатов выполнения задачи.

Исходная цель задачи:
{original_goal}

Критерии формальной проверки:
{validation_criteria}

Результаты выполнения (данные для проверки):
{execution_results}
Инструкция:
1. Проверь результаты на соответствие каждому формальному критерию.
2. Формальные критерии включают: тип данных, структуру, формат, наличие обязательных полей, диапазоны значений.
3. Для каждого критерия укажи, пройден он или нет.
4. Если какой-то критерий не пройден, объясни причину.
Ответ должен быть строго в формате JSON:
{
  "validation_type": "formal",
  "criteria_results": [
    {
      "criterion": "описание критерия",
      "passed": true,
      "expected": "что ожидалось",
      "actual": "что получено",
      "error_details": null
    }
  ],
  "overall_passed": true,
  "can_auto_fix": false,
  "recommendation": "перейти в DONE"
}"""

internal const val INVARIANT_PLAN_CHECK_PROMPT_TEMPLATE = """Ты — ИИ-агент, проверяющий соответствие плана заданным ограничениям.

План задачи (JSON):
{plan_json}

Инварианты — обязательные ограничения, которые план ДОЛЖЕН соблюдать:
{invariants_list}

Последнее изменение от пользователя (пусто = план создан ИИ без правок пользователя):
{user_edit_request}

Инструкция:
1. Для каждого инварианта определи: нарушает ли его данный план?
2. Нарушение есть, если план явно предполагает действия, результаты или подходы, противоречащие ограничению.
3. Если инвариант не применим к плану напрямую — считай его выполненным (passed: true).
4. Критерий прохождения: overall_passed = true, только если ВСЕ инварианты пройдены.
5. Для каждого нарушенного инварианта определи источник нарушения:
   - "ai" — ИИ сам предложил нарушающий элемент (пользователь этого явно не просил)
   - "user" — пользователь явно запросил то, что нарушает инвариант (см. «Последнее изменение»)
   Если поле «Последнее изменение» пусто — источник нарушения всегда "ai".
6. Поле "ai_violated" = true, если хотя бы одно нарушение имеет источник "ai".

Ответ строго в формате JSON:
{
  "invariant_checks": [
    {
      "invariant_key": "ключ инварианта",
      "invariant_value": "значение инварианта",
      "passed": true,
      "violation_details": null,
      "violation_source": "ai"
    }
  ],
  "overall_passed": true,
  "ai_violated": false,
  "summary": "краткое объяснение: что нарушено или почему всё в порядке"
}"""

internal const val VALIDATION_SEMANTIC_PROMPT_TEMPLATE = """Ты — ИИ-агент, выполняющий смысловую проверку результатов выполнения задачи.

Исходная цель задачи (что требовалось получить):
{original_goal}

Контекст задачи (дополнительные требования):
{task_context}

Критерии семантической проверки:
{validation_criteria}

Результаты выполнения:
{execution_results}

Предыдущие шаги и промежуточные результаты:
{previous_steps_results}
Инструкция:
1. Оцени, насколько результат соответствует СМЫСЛУ и ЦЕЛИ задачи, а не только формальным критериям.
2. Проверь на логические ошибки, противоречия, галлюцинации.
3. Оцени полноту ответа (не упущено ли что-то важное).
4. Проверь, решает ли результат исходную проблему пользователя.
5. Если результат неудовлетворительный, определи причину: неверные данные, неполнота, нерелевантность.

Ответ должен быть в формате JSON:
{
  "validation_type": "semantic",
  "analysis": {
    "goal_alignment": 90,
    "completeness": 85,
    "accuracy": 95,
    "coherence": 90
  },
  "issues": [],
  "strengths": ["список сильных сторон результата"],
  "overall_assessment": "good",
  "needs_replanning": false,
  "feedback_for_planning": null,
  "recommendation": "перейти в DONE"
}"""
