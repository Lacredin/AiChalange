package com.example.aiadventchalengetestllmapi.aistateagent

internal const val PLANNING_PROMPT_TEMPLATE = """Ты — ИИ-агент, который составляет пошаговый план выполнения задачи.
Твоя задача — преобразовать запрос пользователя в структурированный JSON-план,
который будет выполнен другими модулями.
Запрос пользователя: {user_query}
Инструкция:
1. Проанализируй запрос и разбей его на логические шаги.
2. Для каждого шага укажи:
   - step_id: уникальный номер (например, 1, 2, 3...).
   - description: что нужно сделать (текст для пользователя).
   - dependencies: массив step_id, которые должны быть выполнены до этого шага.
3. Добавь поле "goal" — краткая формулировка цели (не более 100 символов).
4. Добавь поле "validation_criteria" — массив критериев, которые будут проверяться после выполнения (например, "результат содержит график", "файл сохранен").
5. Если для выполнения не хватает данных или инструментов, задай уточняющий вопрос в поле "clarification_needed" и не заполняй steps.
Ответ должен быть только в формате JSON, без лишнего текста.
Пример структуры ответа:
{
  "goal": "Получить курс доллара и отправить отчет",
  "steps": [
    {
      "step_id": 1,
      "description": "Найти текущий курс USD/RUB на сайте ЦБ",
      "tool": "web_search",
      "input_params": {"query": "usd rub cbrf exchange rate"},
      "dependencies": []
    },
    {
      "step_id": 2,
      "description": "Извлечь курс из текста",
      "tool": "extract_data",
      "input_params": {"source": "step_1_result", "pattern": "курс доллара \\d+"},
      "dependencies": [1]
    }
  ],
  "validation_criteria": [
    "извлеченное значение является числом",
    "число в диапазоне 50-150"
  ],
  "clarification_needed": null
}"""

internal const val PLAN_EDIT_PROMPT_TEMPLATE = """Ты — ИИ-агент, который помогает пользователю скорректировать существующий план выполнения задачи.

Исходный план (JSON):
{original_plan_json}

Пожелания пользователя по изменению:
{user_edit_text}
Инструкция:
1. Проанализируй пожелания пользователя и примени их к исходному плану.
2. Сохрани общую цель (goal) и критерии валидации, если пользователь явно не просит их изменить.
3. При изменениях шагов:
   - Если пользователь просит изменить конкретный шаг (например, "шаг 2 использовать другой инструмент"), обнови соответствующие поля.
   - Если пользователь просит добавить новый шаг, создай его с уникальным step_id (продолжи нумерацию).
   - Если пользователь просит удалить шаг, удали его и обнови зависимости оставшихся шагов.
   - При добавлении или удалении шагов перенумеруй step_id последовательно (1, 2, 3...).
4. После внесения изменений проверь и обнови зависимости:
   - Убедись, что нет зависимостей от удаленных шагов.
   - Если новый шаг должен выполняться после какого-то существующего, добавь соответствующую зависимость.
5. Верни ТОЛЬКО обновленный JSON-план, без пояснений и лишнего текста.
Формат ответа должен быть идентичен структуре исходного плана:
{
  "goal": "цель задачи",
  "steps": [
    {
      "step_id": 1,
      "description": "описание шага",
      "tool": "имя_инструмента_или_null",
      "input_params": {"параметр": "значение"},
      "dependencies": []
    }
  ],
  "validation_criteria": ["критерий1", "критерий2"],
  "clarification_needed": null
}"""
